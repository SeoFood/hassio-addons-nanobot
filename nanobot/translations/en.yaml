---
configuration:
  persistent_data_dir:
    name: Persistent data directory
    description: Folder for all persistent add-on data (config, workspace, WhatsApp auth). Relative paths resolve under /share.
